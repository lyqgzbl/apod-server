global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: apod-server
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - app:8080
    bearer_token: ${METRICS_AUTH_KEY}
